# Vicinae disables layer-shell when XDG_CURRENT_DESKTOP contains "cosmic" (cosmic-comp#1590);
# Ikigai's forked compositor fixes that bug, so give this process a different desktop name.
[Service]
Environment=XDG_CURRENT_DESKTOP=Ikigai QT_QPA_PLATFORM=wayland QT_QPA_PLATFORMTHEME=
RestartSec=1
